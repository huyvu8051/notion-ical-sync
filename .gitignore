target
logs/
.env
backups/
node_modules/
.DS_Store

# wasm-bindgen output for the app crate's hydrate build — built by the
# Dockerfile's wasm-builder stage, not committed.
pkg/

# Compiled Tailwind CSS — built by the Dockerfile's tailwind-builder stage
# from tailwind/*.config.js, not committed.
assets/

# terraform state holds resource attributes in plaintext, including the
# Keycloak client_secret output — must never be committed.
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/terraform.tfvars
